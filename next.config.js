/** @type {import('next').NextConfig} */
const nextConfig = {
  // falls du schon Einstellungen drin hattest, einfach übernehmen
  reactStrictMode: true,
  // ... weitere Optionen
};

module.exports = nextConfig;
